# Create this as the short-lived k-comms-restore-remap Secret only after the
# restored database and object bucket are isolated and ready for verification.
OPERATION_ID=CHANGE_ME_RESTORE_OPERATION_UUID
ACTOR=CHANGE_ME_APPROVED_OPERATOR_IDENTITY
REASON=CHANGE_ME_APPROVED_RESTORE_REASON
