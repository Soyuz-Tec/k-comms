apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: k-comms-edge-ingress
spec:
  # Provider composition replaces this with source ipBlocks that exactly match
  # TRUSTED_PROXY_CIDRS. An empty rule set keeps the neutral overlay fail closed.
  ingress: []
