# Pre-endgame modular-monolith checkpoint

- Captured: 2026-07-17
- Source branch: `agent/mvp-staging-completion`
- Source commit: `e37af3dbe948b473fdbed2158bf8fc5e1f47d0e9`
- Purpose: make the current 95-finding architecture state reproducible before
  the truthful-control-plane and remaining non-media boundary tranches.

## Architecture control-plane evidence

```text
python scripts/validate_architecture.py
Architecture validation passed: 6 classified apps, 1 explicit Repo exceptions,
95 tracked context-boundary violations

python scripts/test_validate_architecture.py
Ran 75 tests
OK
```

SHA-256:

```text
66e22a91c11934bc23a2fd0e36883e07120d1251d6bfaf23618d23088b60d802  docs/02-architecture/context-boundary-baseline.yaml
2aabb4726df04dc5ecfb771818502e36ab1924b03f2b6debe6edc2754d71ef1a  docs/02-architecture/context-boundary-violations.md
0d3b7896cb7b7060b0cf824ffde0a8b73b4139ddb90e62a1f013ad9d3411a922  docs/02-architecture/context-boundaries.yaml
```

## Xref evidence

Umbrella graph:

```text
Tracked files: 238
Compile dependencies: 85
Exports dependencies: 156
Runtime dependencies: 537
Cycles: 10
```

`apps/comms_core` reports four runtime/export file cycles:

1. `accounts.ex` -> `accounts/projector.ex` -> `administration.ex` ->
   `administration/invitations.ex`
2. `integrations/webhook_delivery.ex` -> `integrations/webhook_endpoint.ex` ->
   `integrations/webhook_subscription.ex`
3. `authorization.ex` -> `authorization/deny_all.ex`
4. `accounts/platform_role_grant.ex` -> `accounts/user.ex`

`mix xref graph --format cycles --label compile-connected` reports no cycles.

## Worktree scope

Before this evidence file, the checkpoint contained 311 worktree entries:

- 185 modified files
- 3 deleted files
- 123 untracked files

The checkpoint explicitly includes the existing audio/video implementation,
including the AudioCalls core modules and migrations, LiveKit integration,
web/worker adapters, client call features, tests, and ADRs 0024 and 0025.
Those files establish the current product checkpoint but are frozen for the
non-media modularization tranche. The only permitted later media-file change is
a mechanically required canonical-Tenant module reference update; media
behavior and the Calls boundary remain deferred.

## Next-tranche rule

No domain-boundary refactor may begin until the validator attributes every
production module, generically enforces runtime collaborations and explicit
deferrals, and emits separate compiled, runtime, and combined graphs.
