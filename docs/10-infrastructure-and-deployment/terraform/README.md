# Terraform Reference Area

This documentation repository does not contain production infrastructure code. The eventual infrastructure repository should follow the module boundaries in `infrastructure-as-code.md`, pin providers, test modules, and keep environment state separate.
